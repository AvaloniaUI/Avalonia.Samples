<Project>
  <PropertyGroup>
    <Nullable>enable</Nullable>
    <!-- Make all projects in this repo trim-safe and compatible with AOT by default -->
    <IsTrimmable>true</IsTrimmable>
    <IsAotCompatible>true</IsAotCompatible>
  </PropertyGroup>
</Project>
